
Create an Array of Interleaved Numbers
// Write an alternationArray function that takes a non-negative integer n as an argument and returns an array,
// containing numbers from 0 to n, where even numbers are positive and odd numbers are negative.

function alternationArray (n) {
let arr = [];
for (let i = 0; i <= n; i ++) {
if (i% 2 === 0)
arr.push (i);
else if (i% 1 === 0)
 arr.push (-i);
}
return arr;
}
  console.log (alternationArray (5));
 
