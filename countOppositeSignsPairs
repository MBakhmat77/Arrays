
The number of pairs of elements with different signs
// Write a function countOppositeSignsPairs that takes an arr array as an argument,
// and returns the number of pairs of adjacent elements that have different signs.
function countOppositeSignsPairs (arr) {
let count = 0;
for (let i = 0; i <arr.length; i ++) {
if (arr [i]> 0 && arr [i + 1] <0 || arr [i] <0 && arr [i + 1]> 0)
count ++;
}
return count;
